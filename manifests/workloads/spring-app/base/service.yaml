# Spring Boot Service File
apiVersion: v1
kind: Service
metadata:
  name: spring-app-service
  labels:
    app: spring-app
spec:
  selector:
    app: spring-app
  ports:
    - name: http-web
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer